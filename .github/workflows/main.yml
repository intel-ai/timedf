name: Benchmarking
on:
  schedule:
    - cron: '51 1 * * *'
  workflow_dispatch:
    inputs:
      reset-cache:
        type: boolean
        default: true
  push:

jobs:
#  modin-containerized:
#    uses: ./.github/workflows/test-docker.yml
#    with:
#      name: modin-containerized
#      reset-cache: ${{ !!inputs.reset-cache }}
#    secrets:
#      token: ${{ secrets.AMYACHEV_GITHUB_TOKEN }}
#
  modin-ray:
    uses: ./.github/workflows/test-modin-ray.yml
    with:
      name: modin-ray
      reset-cache: ${{ !!inputs.reset-cache }}
    secrets:
      DB_COMMON_OPTS: ${{ secrets.DB_COMMON_OPTS }}

